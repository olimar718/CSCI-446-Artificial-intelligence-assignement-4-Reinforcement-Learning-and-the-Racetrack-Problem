
public class Qlearning {
    public Qlearning(Racecar racecar, char[][] course){
        //enumerate all state action pair -> need a state class ? 
        //initialize Qtable randomly

        //repeat this until ...
            //start picking an action acording to current Qtable
            //update Qtable for the state action pair according to reward + discount factor and step size
            //repeat
    }
}